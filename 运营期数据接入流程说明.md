# 运营期数据接入与关联修订

更新：2026-09-11。新增 `operations.html#setup`，作为现有运营展示的配置入口，不改变三个设施和已有资产映射。

## 官方依据

- [Files / Adding Models](https://help.autodesk.com/view/TANDEM/ENU/?guid=tandem-files)：模型可从本地 Revit / IFC 或 Forma Data Management 导入。源版本、模型对齐与默认可见性需要明确。本原型只保存文件名与来源方案，不解析几何；并不声称任意区域的外部项目可连接。
- [Assets](https://help.autodesk.com/cloudhelp/ENU/Tandem-Facilities/files/tandem-assets.html)：设施模板定义资产信息模型，可把源模型属性映射到资产参数。原型把业务资产编码与模型元素标识分开，并演示制造商字段来源选择。
- [Key Concepts](https://help.autodesk.com/cloudhelp/ENU/Tandem-Getting-Started/files/tandem-key-concepts.html)：连接表示 IoT 设备或资产，数据流记录读数。模型、业务资产、连接与流的标识不能混为一个编号。
- [Create and configure connections](https://www.autodesk.com/learn/ondemand/tutorial/create-and-configure-connections)：配置连接接收传感器数据，管理载荷与参数映射。
- [Tandem Connect integrations](https://intandem.autodesk.com/integrations-tandem-connect/)：集成服务可通过 HTTP、MQTT 等连接外部数据。原型只展示网关 / API 与集成服务两种路径，不模拟真实鉴权。

## 页面适配

“数据接入与关联”是为本项目增加的引导工作台，不是对 Tandem 导航名称的逐字复制。

1. 导入源模型：来源 → 导入设置 → 保存草稿。可用本地文件名或预置样例。
2. 关联资产数据：选择现有资产 → 核对来源字段 → 确认模型元素、业务编码、空间、系统和文档。
3. 接入传感器：宿主与测点 → 数值 / 时间戳 / 单位 → 样例载荷。保留未接入、离线、预置读数的区别。
4. 核验并使用：分别显示来源缺口、字段完整性、配置草稿和运行入口。

右侧为当前预置资产的来源链；左侧为本次配置草稿，两者没有自动合并。草稿按设施隔离，仅当前页面内保留。保存草稿不代表真实导入、业务批准、正式交付或设备在线。

原有模型与文档的导入、数据交付的映射、连接与数据流的传感器入口均接入该流程；顶栏问号 README 同步更新。源模型的“已导入”标签改为“预置模型”，避免与本次导入操作混淆。
